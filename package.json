{
  "name": "astroprisma",
  "version": "0.1.0",
  "private": true,
  "description": "An unofficial implementation of ASTROPRISMA.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && esbuild src/astroprisma.ts --bundle --minify --outfile=dist/astroprisma.js --platform=browser --format=esm --sourcemap --tsconfig=tsconfig.json && cp system.json dist/ && cp -r lang styles ui templates dist/",
    "watch": "esbuild src/astroprisma.ts --bundle --outfile=dist/astroprisma.js --platform=browser --format=esm --sourcemap --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "fvtt-types": "github:League-of-Foundry-Developers/foundry-vtt-types#main",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
